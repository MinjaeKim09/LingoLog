rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The app never reads Firestore directly; Cloud Functions use the Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
